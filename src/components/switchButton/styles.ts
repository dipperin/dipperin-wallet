import { createStyles } from '@material-ui/core/styles'

const styles = createStyles({
  switchBase: {
    height: 20
  },
  switchBar: {
    height: 20,
    borderRadius: 20,
    marginTop: -10
  },
  aa: {}
})
export default styles
