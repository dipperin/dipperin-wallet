import { createStyles } from '@material-ui/core/styles'

const styles = createStyles({
  smartContract: {
    position: 'relative'
  },
  container: {
    width: 892,
    height: 450,
    margin: '0 auto',
    borderRadius: 6,
    background: '#fff',
    overflow: 'auto'
  }
})

export default styles
